"""
This program Create this structure correctly so that `(3, 4)` remains one single element:  
`1, [True, False], (3, 4)`  
Print the full tuple and its length.
"""

nested_tuple = (1, [True, False], (3, 4))

print(f"Tuple: {nested_tuple}")
print(f"Length: {len(nested_tuple)}")