"""
This program create a tuple: `(10, 20, 30, 40, 50)`  
Print the first item, the third item, and the last item (use negative indexing for the last one).
"""

numbers = (10, 20, 30, 40, 50)

print(numbers[0])
print(numbers[2])
print(numbers[-1])